TABELLA: tenant_billing
DESCRIZIONE: Tabella per la gestione di tenant_billing

CAMPI UTILIZZABILI (USA ESATTAMENTE QUESTI NOMI):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IDENTIFICATIVI:
- id (integer): Campo id [PRIMARY KEY]

RELAZIONI PRINCIPALI:
- tenant_id (text): Campo tenant_id [REQUIRED] [FK verso tenants]

ALTRI CAMPI:
- plan (text): Campo plan [REQUIRED]
- amount (double precision): Campo amount [REQUIRED]
- currency (text): Campo currency [REQUIRED] [DEFAULT: EUR]
- billingPeriod (text): Campo billingPeriod [REQUIRED]
- invoiceUrl (text): Campo invoiceUrl
- createdAt (timestamp without time zone): Campo createdAt [REQUIRED] [DEFAULT: CURRENT_TIMESTAMP]

STATI E FLAGS:
- status (text): Campo status [REQUIRED]

DATE E TIMESTAMP:
- startDate (timestamp without time zone): Campo startDate [REQUIRED]
- endDate (timestamp without time zone): Campo endDate [REQUIRED]
- updatedAt (timestamp without time zone): Campo updatedAt [REQUIRED]

RELAZIONI:
━━━━━━━━━━
- tenants (N:1): Collegamento via tenant_id
  * ON DELETE: RESTRICT
  * ON UPDATE: CASCADE

QUERY COMUNI:
━━━━━━━━━━━━

1. Recupera tutti i record di tenant_billing per un tenant:
{
  "table": "tenant_billing",
  "select": [
    "id",
    "tenant_id",
    "plan",
    "amount",
    "currency"
  ],
  "where": {
    "tenant_id": "uuid_tenant"
  },
  "limit": 100
}

2. Recupera tenant_billing con dati di tenants:
{
  "table": "tenant_billing",
  "select": [
    "*"
  ],
  "leftJoin": [
    {
      "table": "tenants",
      "first": "tenant_billing.tenant_id",
      "second": "tenants.id"
    }
  ],
  "limit": 50
}

NOTE IMPORTANTI:
━━━━━━━━━━━━━━━
- Tabella multi-tenant: tutti i record sono isolati per tenant_id
